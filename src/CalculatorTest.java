public class CalculatorTest {
    public static void main(String[] args) {
        Main main = new Main();
        main.startCalculator(main.scanner, main.calculatorInfo, main.calculatorFunctions);
    }
}
